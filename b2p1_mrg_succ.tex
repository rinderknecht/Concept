%%-*-latex-*-

\documentclass[11pt]{article}

\usepackage{pst-tree,pst-eps}

\begin{document}

\TeXtoEPS
%\psset{showbbox=true}
\pstree[nodesep=2pt,levelsep=25pt,treesep=6pt,arrows=<-]{\TR{\(\sum_{j=q}^{r}{2^{e_{j}}}+2^q\)}}{
  \TR{\(2^{e_r}\)}
  \Tn
  \psset{nodesep=1pt,linestyle=dotted}
  \pstree[nodesep=2pt,linestyle=solid]{\TR{\(2^{e_q}+2^q\)}}{
    \TR{\(2^{e_{q}}\)}
    \Tn
    \Tn
    \Tn
    \TR{\(2^{q}\)}
  }
}
\endTeXtoEPS

\end{document}
